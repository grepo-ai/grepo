[project]
name = "grepo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pip==25.1.1",
    "tree-sitter==0.24.0",
    "uv==0.7.13",
]
