#!/usr/bin/env bun
/**
 * Single-command entry for distribution. Run with: grepo
 * Requires Bun. The UI spawns the Python backend (grepo-stdio or python -m cli.backend_stdio).
 */
import "../src/index.tsx";
